# Isolation-coverage scanner runtime dependencies.
#
# Runtime: none — the scanner uses only the Python standard library so
# it works on any recent Python 3 without a venv.
#
# Dev/test: pytest for the test_check_isolation.py suite.
pytest>=7.4.0
