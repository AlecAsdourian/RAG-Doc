[project]
name = "smart-docs-workers"
version = "0.1.0"
description = "ML and RAG processing workers for Smart Documentation Platform"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 100
target-version = ['py311']
