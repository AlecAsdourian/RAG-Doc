# pkg/auth

JWT validation, tenant middleware, webhook provisioning, and OAuth
handlers for the Go backend.

For the tenant isolation pattern (middleware primitive, DB trigger,
isolation-test convention, CI gate), see [`docs/isolation.md`](../../../../docs/isolation.md).
