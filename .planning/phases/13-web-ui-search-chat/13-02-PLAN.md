---
phase: 13-web-ui-search-chat
plan: 02
type: execute
---

<objective>
Build the search UI with search bar, results list, and code preview cards.

Purpose: Let users search the codebase and see relevant results with code previews.
Output: Functional search interface that calls the backend API and displays results.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-web-ui-search-chat/13-CONTEXT.md
@.planning/phases/13-web-ui-search-chat/13-01-SUMMARY.md

# Reference UI components:
@.planning/phases/13-web-ui-search-chat/front-end_reference/components/search-header.tsx
@.planning/phases/13-web-ui-search-chat/front-end_reference/components/search-results.tsx
@.planning/phases/13-web-ui-search-chat/front-end_reference/components/code-block.tsx

# API types:
@services/frontend/src/lib/api/types.ts

**Search requirements from CONTEXT.md:**
- Cards with code preview that expand on click
- Show file path and line numbers
- Ranked by relevance
- Fast, relevant results
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create search input component</name>
  <files>services/frontend/src/components/SearchInput.tsx</files>
  <action>
Create search input with:
- Input field with search icon (lucide-react Search icon)
- Debounced input (300ms delay before triggering search)
- Loading state indicator
- Clear button when text present
- Keyboard shortcut hint (Cmd/Ctrl + K)

Use shadcn Input component as base. Style clean and minimal.

```typescript
interface SearchInputProps {
  value: string;
  onChange: (value: string) => void;
  isLoading?: boolean;
  placeholder?: string;
}
```

Include focus ring, proper padding for icon positioning.
  </action>
  <verify>Component renders, typing triggers onChange, loading spinner shows</verify>
  <done>Search input with debounce and loading state working</done>
</task>

<task type="auto">
  <name>Task 2: Create code preview card component</name>
  <files>services/frontend/src/components/CodeCard.tsx, services/frontend/src/components/CodeBlock.tsx</files>
  <action>
Create CodeBlock component for syntax highlighting:
- Use a simple CSS-based approach (no heavy syntax highlighter)
- Line numbers on left
- Monospace font (font-mono)
- Highlight matched lines with subtle background

Create CodeCard component showing search result:
- File path with icon (based on language)
- Breadcrumb path if available
- Relevance score badge
- Collapsed preview (first 5 lines)
- Expand on click to show full content
- Line number range (e.g., "Lines 42-67")

```typescript
interface CodeCardProps {
  result: ChunkResult;
  isExpanded: boolean;
  onToggle: () => void;
  highlightQuery?: string;
}
```

Use shadcn Card as base. Add subtle hover state, smooth expand animation.
  </action>
  <verify>Card renders with code preview, expands/collapses on click</verify>
  <done>Code cards with preview and expand working</done>
</task>

<task type="auto">
  <name>Task 3: Create search results list and wire up API</name>
  <files>services/frontend/src/components/SearchResults.tsx, services/frontend/src/hooks/useSearch.ts, services/frontend/src/App.tsx</files>
  <action>
Create useSearch hook:
```typescript
function useSearch(repositoryId: string) {
  const [query, setQuery] = useState('');
  const [results, setResults] = useState<ChunkResult[]>([]);
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  // Debounced search effect
  useEffect(() => {
    if (!query.trim()) { setResults([]); return; }

    const timeout = setTimeout(async () => {
      setIsLoading(true);
      try {
        const res = await apiClient.search({ query, repository_id: repositoryId });
        setResults(res.results);
      } catch (e) {
        setError(e instanceof Error ? e.message : 'Search failed');
      } finally {
        setIsLoading(false);
      }
    }, 300);

    return () => clearTimeout(timeout);
  }, [query, repositoryId]);

  return { query, setQuery, results, isLoading, error };
}
```

Create SearchResults component:
- Empty state when no query
- Loading skeleton cards
- No results message
- List of CodeCard components with expand state management

Update App.tsx:
- Add SearchInput at top
- Add SearchResults below
- Use a hardcoded repositoryId for now (will add repo selector later)
  </action>
  <verify>Typing in search triggers API call (check network tab), results display</verify>
  <done>Search UI complete with API integration, results display</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Search input triggers debounced API calls
- [ ] Results render as code cards
- [ ] Cards expand/collapse on click
- [ ] Loading state shows during fetch
- [ ] Empty state shows when no query
- [ ] Error state handles API failures gracefully
</verification>

<success_criteria>

- All tasks completed
- Search input with debounce working
- Code cards render with previews
- API integration complete
- Clean, responsive design
</success_criteria>

<output>
After completion, create `.planning/phases/13-web-ui-search-chat/13-02-SUMMARY.md`
</output>
